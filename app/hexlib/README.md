## Python wrapper around libhexp for HexParticle

* HexParticle searches for ```libhexp``` on ```/usr/local/lib/HexParticle``` by default.
* Use the ```-l``` or ```--lib-path``` option to specify the library path if the library is compiled in some other directory.